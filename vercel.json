{
  "buildCommand": "cd trustlink-frontend && npm install && npm run build",
  "outputDirectory": "trustlink-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
